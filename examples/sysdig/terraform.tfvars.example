# You should update the below variables

# aws_region from the core-infra example
aws_region      = "us-west-2"

# It is optional to change the below variables
# core_stack_name is also same as ecs_cluster_name
core_stack_name =   "ecs-blueprint-infra"
service_name    =   "ecs-infected-backend-demo"
namespace       =   "default"
desired_count   =   3
cpu             =   256
memory          =   512
container_name  =   "ecs-infected-backend-demo"   # Sample container name
container_image =   "sysdiglabs/writer-to-bin"    # Sample "backend" workload that we want to secure
container_port =    3000                          # Container port example

#Sysdig Access Token
sysdig_access_key     = "provide-agent-token-here"          # Sysdig UI > Settings > Agent Installation > Your Access Key
sysdig_collector_url  = "ingest-us2.app.sysdig.com"         # https://docs.sysdig.com/en/docs/administration/saas-regions-and-ip-ranges
sysdig_collector_port = "6443"
sysdig_secure_api_token = "provide-apikey-here"             # Sysdig UI > Settings > User Profile > Sysdig Secure API Token"

#Sysdig workload agent configured as a sidecar
sidecar_container_definition = {
  image      = "quay.io/sysdig/workload-agent:latest"
  name       = "SysdigInstrumentation"
  entrypoint = ["/opt/draios/bin/logwriter"]
}
